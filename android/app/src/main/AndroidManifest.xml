<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- এখানে package="" লাইনটা থাকবে না -->

    <!-- এই পারমিশনগুলো যোগ করো (Android 13+ এর জন্য নতুন সিস্টেম) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" /> <!-- Android 13+ এ লাগে না -->

    <!-- Android 13+ (API 33+) এর জন্য নতুন গ্রানুলার পারমিশন (সবচেয়ে ভালো প্র্যাকটিস) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" /> --> <!-- ভিডিও লাগলে -->
    <!-- <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" /> -->

    <application
        android:label="football_firebase"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Flutter Plugin Registration -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>

    <!-- এই queries টা ঠিক আছে, রাখো -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>